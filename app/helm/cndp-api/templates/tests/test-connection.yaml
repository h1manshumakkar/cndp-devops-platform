apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cndp-api.fullname" . }}-test-connection"
  labels:
    {{- include "cndp-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cndp-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
